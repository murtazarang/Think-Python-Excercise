git checkout -b branchname

git branch

git checkout master

Markdown Syntax

the file needs a *.md file

## Seondary heading
### Tertiary Heading

These gives a larger text font

Unordered Bullet Lists are given by '*'

install.packages("Nameofpackage")

head(rownames(a), 3) -> a is available packages and 3 is the first 3 packages that are available.

After installing the package, if need to load the package to make it available. You need to use the library command.

library(xxx) is to be used for it

if you use the search() command after using library() command, you get all the packages that are loaded for it.